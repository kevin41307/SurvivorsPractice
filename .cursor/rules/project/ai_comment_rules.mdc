---
alwaysApply: true
---

# 專案註解規範

## 核心原則

1.  註解解釋「為什麼」，不是「做什麼」
2.  優先使用良好命名取代註解
3.  註解必須與程式碼同步
4.  註解的位子應該排在類別或方法的屬性標記前面。

    範例
    ``` csharp
    /// Intent:
    /// 說明此段邏輯的「目的」與「存在理由」

    /// Invariant:
    /// 描述必須永遠成立的條件（應可被測試驗證）

    /// Why:
    /// 解釋非直覺設計或取捨原因
    public void Something()
    {
    
    }
    ```

    範例
    ``` csharp
    /// Intent:
    /// 說明此段邏輯的「目的」與「存在理由」

    /// Invariant:
    /// 描述必須永遠成立的條件（應可被測試驗證）

    /// Why:
    /// 解釋非直覺設計或取捨原因
    [Test]
    public void Something()
    {
    
    }
    ```
------------------------------------------------------------------------

## 1. 註解結構

- 適用於所有類別, 介面。

``` csharp
/// Intent:
/// 說明此段邏輯的「目的」與「存在理由」

/// Invariant:
/// 描述必須永遠成立的條件（應可被測試驗證）

/// Why:
/// 解釋非直覺設計或取捨原因
```

------------------------------------------------------------------------

## 1. 方法註解結構

- 適用於所有方法。
- 必須包含 `Intent`、`Invariant`、`Why`。
- 包含 `SideEffects`，若無，可以不寫。

``` csharp
/// Intent:
/// 說明此段邏輯的「目的」與「存在理由」

/// Invariant:
/// 描述必須永遠成立的條件（應可被測試驗證）

/// SideEffects:
/// 列出所有副作用（EventBus、狀態修改、IO）

/// Why:
/// 解釋非直覺設計或取捨原因
```


------------------------------------------------------------------------

## 2. 註解結構

適用於方法內實作細節。

``` csharp
// Intent:
// 說明此段邏輯的「目的」與「存在理由」
```

------------------------------------------------------------------------

## 3. TODO / FIXME / HACK

``` csharp
// TODO (kevin): 支援 stackable buff
// FIXME: allocation issue
// HACK: 暫時 workaround
```

------------------------------------------------------------------------

## 11. 語言(強制)

-   盡量使用繁體中文, 除非是英文專有名詞或技術術語或類別, 方法名稱, 變數名稱...。

------------------------------------------------------------------------